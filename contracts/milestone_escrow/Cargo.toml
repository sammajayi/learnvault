[package]
name = "milestone-escrow"
description = "Milestone-based escrow for scholarship disbursements"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false
version.workspace = true

[package.metadata.stellar]
cargo_inherit = true

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
learnvault-shared = { workspace = true }
soroban-sdk = { workspace = true }
stellar-registry = "0.0.4"

[dev-dependencies]
learnvault-shared = { workspace = true, features = ["testutils"] }
soroban-sdk = { workspace = true, features = ["testutils"] }
proptest = { workspace = true }

[[test]]
name = "fuzz_escrow_operations"
path = "fuzz/fuzz_targets/escrow_operations.rs"
harness = true
